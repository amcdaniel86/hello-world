hello!

goodbye now!

Sherlock Holmes pointed out, When you have eliminated the impossible, whatever remains, however improbable, must be the truth.

input function returns a string, must convert the string into an integer in order for Python to mathematically add the user inputs together.

The temperature is 40
The temperature is 89
The temperature is 64
The temperature is 41
The temperature is 89
The temperature is 40
modulus numbers if less than the denominator, a fraction... the top divisor will be same amount as the  remainder or modulus answer

